<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= content_for?(:title) ? yield(:title) : "Moorem" %> - Web Development using RoR, PHP and Mobile Application
    Development
  </title>
  <%= csrf_meta_tags %>

  <!-- Le HTML5 shim, for I 	E6-8 support of HTML elements -->
  <!--[if lt IE 9]>
  <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
  <![endif]-->

  <%= stylesheet_link_tag "application", :media => "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag 'http://code.jquery.com/jquery-1.8.3.min.js' %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>

  <!-- For all other devices -->
  <!-- Size should be 32 x 32 pixels -->
  <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon">
  <link rel="icon" href="/favicon.ico" type="image/x-icon">


</head>
<body class="home">
<div class="wrap">
  <%= render 'shared/header' %>
  <% flash.each do |name, msg| %>
      <% if msg.is_a?(String) %>
          <div class="alert alert-<%= name == :notice ? 'success' : 'danger' %>">
            <a class="close" data-dismiss="alert">&#215;</a>
            <%= content_tag :div, msg, :id => "flash_#{name}" %>
          </div>
      <% end %>
  <% end %>
  <%= yield %>
  <%= render 'shared/footer' %>
  <!-- Scroll To Top -->
  <a href="#" class="scrollup"><i class="icon-angle-up"></i></a>
</div>
<!-- Wrap End -->

<%= javascript_include_tag "/js/custom.js", :media => "all", "data-turbolinks-track" => true %>
</body>
</html>
